# DNS Enumeration Tool
# Connor Dudarchik
# Usage: enumdns input_file

# Now trying to get a variable input text file
foreach($line in Get-Content $args[0])
{
	Write-Host ($line)
}
# That was easy. Also works with different folders.
